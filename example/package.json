{
  "name": "@benqoder/beam-example",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run build:beam && vite build && vite build --mode client",
    "build:beam": "cd .. && npm run build",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@benqoder/beam": "file:..",
    "capnweb": "^0.4.0",
    "hono": "^4.6.0",
    "honox": "^0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@hono/vite-build": "^1.0.0",
    "@hono/vite-dev-server": "^0.17.0",
    "typescript": "^5.0.0",
    "vite": "^5.4.0",
    "wrangler": "^3.91.0"
  }
}
